#{param name=repositories/}

	<% for (def repository : repositories) { %>

	<p>
	  ${repository.name}<br/>
	  Created date: ${repository.created_at}<br/>
	  Description:<br/>
	  ${repository.description}
	</p>

	<% } %>
